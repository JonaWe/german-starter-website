import NextStep from "./NextStep";
import LastStep from "./LastStep";

export default function LastStep({next, previous}) {
  return (
    <div>
      <LastStep />
      <NextStep />
    </div>
  );
}
